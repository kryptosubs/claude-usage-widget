# Line endings are deliberate: Windows hosts need CRLF (cmd.exe misparses the
# caret continuations in .cmd files with LF), the WSL script needs LF.
* text=auto
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.vbs text eol=crlf
*.md  text eol=crlf
*.sh  text eol=lf
